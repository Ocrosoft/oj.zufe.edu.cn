#include<stdio.h>
#include<string.h>
#include<ctype.h>
int main()
{
    char s[100]={0};
    int i,len,num;
    while(gets(s)!=NULL)
    {
    len=strlen(s);
    num=0;
    for(i=0;i<len;i++)
    {
        if(isalpha(s[i]))
            num++;
    }
    printf("%d\n",num);
    }
    return 0;
}
