#include <stdio.h>
#include <stdlib.h>

int main()
{
    int sum=0,n,m;
    scanf("%d",&n);
    int i=0;
    for(i=0;i<n;i++)
    {
        scanf("%d",&m);
        if (m%2==0)
        {
            sum+=m;
        }
    }
    return 0;
}
